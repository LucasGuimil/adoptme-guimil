export const missingDataErrorPetInfo = (data)=>{
    return `One or more properties are incomplete.
    List of required properties: name, specie, birthDate.`
}

export const missingDataErrorUserInfo = (data)=>{
    return `One or more properties are incomplete.
    List of required properties: first_name, last_name, email, password.`
}

